@using ACSAdminWeb.Models
@if(Request.IsAuthenticated) {
    UserAuthentication userAuth = new UserAuthentication();
    string EmployeeGroup = userAuth.GetEmployeeGroup(@User.Identity.Name);
    <text><strong> Group - @EmployeeGroup</strong></text>
}
